/* Replace the count of comments */	
$("#comments_count").html("<%= pluralize(@product.comments.count, 'Comment') %>");
$('#<%= dom_id(@comment) %>').css('background', 'red');
$('#<%= dom_id(@comment) %>').hide("fade", {}, 1500);
