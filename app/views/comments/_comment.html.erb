<%= content_tag_for(:span, comment) do %>
  <p><b><%= comment.user.email %></b> says that: </p> <br/>
  <p><%= comment.content %></p>
  <p class="age" >Posted at <%= time_ago_in_words(comment.created_at) %> ago.
  (<%= link_to 'Delete', product_comment_path(@product, comment), :confirm => 'Are you sure?', :method => :delete, :remote => true %>)</p>
<% end %>
