<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @category.name %>
</p>
<div>
  <% @category.products.each do |product| %>
    <span>
      <p><%= product.name%> | <%= link_to 'show', product_path(product)%> | <%= link_to 'Edit', edit_product_path(product)%> </p>      
    </span>
      
  <%end%>
</div>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
